import javax.swing.*;

public class Admin extends JFrame {
    private TuitionCalculatorUI parentUI;

    public Admin(TuitionCalculatorUI parentUI) {
        this.parentUI = parentUI;
    }
}
